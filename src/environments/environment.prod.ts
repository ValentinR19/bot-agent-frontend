export const environment = {
  production: true,
  apiUrl: 'https://api.bot-agent.com/api/v1', // Ajustar según el dominio de producción
  tenantId: '', // Se debe configurar según el tenant actual
};
