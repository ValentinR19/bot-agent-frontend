import { Routes } from '@angular/router';
import { TeamsFormComponent } from './teams-form.component';

export const TEAMS_FORM_ROUTES: Routes = [
  {
    path: '',
    component: TeamsFormComponent,
  },
];
