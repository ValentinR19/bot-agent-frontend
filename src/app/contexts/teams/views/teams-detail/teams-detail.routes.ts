import { Routes } from '@angular/router';
import { TeamsDetailComponent } from './teams-detail.component';

export const TEAMS_DETAIL_ROUTES: Routes = [
  {
    path: '',
    component: TeamsDetailComponent,
  },
];
