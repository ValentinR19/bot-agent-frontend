import { Routes } from '@angular/router';
import { TeamsListComponent } from './teams-list.component';

export const TEAMS_LIST_ROUTES: Routes = [
  {
    path: '',
    component: TeamsListComponent,
  },
];
