import { Routes } from '@angular/router';
import { RolesListComponent } from './roles-list.component';

export const ROLES_LIST_ROUTES: Routes = [
  {
    path: '',
    component: RolesListComponent,
  },
];
