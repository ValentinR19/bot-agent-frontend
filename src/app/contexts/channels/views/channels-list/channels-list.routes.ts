import { Routes } from '@angular/router';
import { ChannelsListComponent } from './channels-list.component';

export const CHANNELS_LIST_ROUTES: Routes = [
  {
    path: '',
    component: ChannelsListComponent,
  },
];
