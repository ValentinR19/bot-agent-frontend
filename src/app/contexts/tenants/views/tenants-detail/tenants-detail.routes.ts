import { Routes } from '@angular/router';
import { TenantsDetailComponent } from './tenants-detail.component';

export const TENANTS_DETAIL_ROUTES: Routes = [
  {
    path: '',
    component: TenantsDetailComponent,
  },
];
