import { Routes } from '@angular/router';
import { TenantsFormComponent } from './tenants-form.component';

export const TENANTS_FORM_ROUTES: Routes = [
  {
    path: '',
    component: TenantsFormComponent,
  },
];
